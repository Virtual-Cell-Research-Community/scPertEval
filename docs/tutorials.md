# Tutorials

Each tutorial is a runnable notebook, executed when the docs are built, so the output shown
is real. They generate a tiny synthetic dataset inline and need no downloads.

```{toctree}
:maxdepth: 1

tutorials/cli-walkthrough
```

More coming soon:

- **Python API** — use scPertEval programmatically from a notebook or script
- **Extending scPertEval** — add a new protocol, feature space, or control source
