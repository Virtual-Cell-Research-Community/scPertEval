# Tutorials

Each tutorial is a runnable Jupyter notebook with its outputs saved, so it renders here and
can be downloaded (or opened in Colab) and re-run. They generate a tiny synthetic dataset
inline and need no downloads.

```{toctree}
:maxdepth: 1

tutorials/cli-walkthrough
```

More coming soon:

- **Python API** — use scPertEval programmatically from a notebook or script
- **Extending scPertEval** — add a new protocol, feature space, or control source
